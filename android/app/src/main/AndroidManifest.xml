<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- UsageStatsManager (granted via Settings UI) -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Foreground service for ReelCounter accessibility/overlay (Phase B) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Draw the HUD bubble over other apps. Granted via system Settings UI. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Battery exemption deeplink (granted via Settings) -->
    <uses-permission
        android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"
        tools:ignore="BatteryLife" />

    <!-- Boot receiver hook for re-arming counter (optional; user must whitelist on OEMs) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
      Android 11+ package visibility. We must declare the apps we want to inspect
      (label / icon / installed-state) for the per-app dashboard breakdown.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>
        <package android:name="com.instagram.android" />
        <package android:name="com.google.android.youtube" />
        <package android:name="com.zhiliaoapp.musically" />
        <package android:name="com.ss.android.ugc.trill" />
        <package android:name="com.snapchat.android" />
        <package android:name="com.facebook.katana" />
        <package android:name="com.facebook.lite" />
    </queries>

    <application
        android:label="ScrollIQ"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              Referral / invite deep links.
              Custom scheme works out of the box (good for testing & sharing):
                  scrolliq://invite?ref=CODE
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="scrolliq" android:host="invite" />
            </intent-filter>

            <!--
              HTTPS App Link: https://scrolliq.app/invite?ref=CODE
              autoVerify requires hosting a Digital Asset Links file at
              https://scrolliq.app/.well-known/assetlinks.json. Until then the
              link opens in the browser instead of the app.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="scroll-iq.vercel.app"
                    android:pathPrefix="/invite" />
            </intent-filter>
        </activity>

        <!-- Reel-counting AccessibilityService -->
        <service
            android:name=".reelcounter.ReelCounterAccessibilityService"
            android:exported="false"
            android:label="@string/reel_counter_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!--
          Foreground service that draws the ScrollIQ HUD bubble on top of the
          current foreground app. specialUse type avoids tying us to a system
          category that doesn't fit (the bubble is neither media projection
          nor data sync). Required property declares the subtype.
        -->
        <service
            android:name=".reelcounter.OverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="reel_counter_overlay" />
        </service>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- PostHog -->
        <meta-data
            android:name="com.posthog.posthog.API_KEY"
            android:value="@string/posthog_api_key" />
        <meta-data
            android:name="com.posthog.posthog.POSTHOG_HOST"
            android:value="@string/posthog_host" />
        <meta-data
            android:name="com.posthog.posthog.TRACK_APPLICATION_LIFECYCLE_EVENTS"
            android:value="true" />
    </application>
</manifest>
